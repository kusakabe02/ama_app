<h1>ユーザー情報編集</h1>

<!-- 他ユーザーの編集画面も開けなくする -->
<% if current_user == @user %>

  <%= render 'form', user: @user %>

<% else %>
  <h2>この情報は閲覧できません！</h2>
<% end %>

<%= link_to 'Show', @user　%> |
<%= link_to '戻る', users_path %>
